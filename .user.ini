; PHP upload limits for the admin image uploader (cPanel / PHP-FPM).
; Product & color images are capped at 3MB each in code; allow headroom
; so PHP itself does not silently reject them before the app runs.
upload_max_filesize = 8M
post_max_size = 32M
max_file_uploads = 30
